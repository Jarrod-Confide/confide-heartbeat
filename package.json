{
  "name": "uptime-monitor",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Workers cron-based uptime monitor for Confide services. Posts to #system-status on state transitions.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.78.0"
  }
}
